import Lobby from './components/Lobby';

function App() {
    return (
        <div className="App">
            {/* <button onClick={ () => { socket.connect()}}>Conectar</button> */}
            <Lobby />
        </div>
    );
}

export default App;